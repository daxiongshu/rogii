# V5 batch-2 run-17 source review

Run 17 starts after the clean categorical recovery in Run 16 selected the
exact D209 fallback.  The strongest uncontaminated selection-clean parent is
the older C016 ensemble at RMSE `5.739422850`, a `+0.200768160` gain versus
protected V20.  Run 17 therefore targets the remaining `0.299231840` with
several complementary stacked models rather than another global direction.

The active quarantine forbids `purece_strat_clean.npz`, its selector and
routes, and every transitive Run-10 through Run-13 artifact.  None is a
parent, candidate, feature, label, selector input, or fallback here.  The
registered expert pool is restricted to clean Run-6 archives created before
the invalid selector and the valid Run-8 C016 lineage.  Every archive has the
same 619-well F0--F3 layout and false protected flags.  A metric-free
manifest must re-hash and re-audit those facts before any Run-17 target
construction.

Run 14's 253-column block cache is also eligible: it was built from immutable
V20 component, GR-likelihood, geometry, and prediction-only evidence before
loading truth, and its record explicitly reports `truth_array_loaded=false`.
Run 17 may augment those features only with summaries of the registered clean
expert predictions.

## Registered mechanisms

1. A multi-output block expert-utility router predicts which clean expert
   correction is useful at each complete-well block.  Ridge, ExtraTrees, and
   CatBoost variants are decoded through conservative positive margins,
   shares, and exact C016 fallback.
2. A block control stacker predicts an anchored low-frequency residual to
   C016 from the same inference-only evidence.  Its targets are bounded
   complete-block basis controls, not row identities.
3. A row/sequence residual stack uses registered clean-expert disagreement,
   immutable organizer inputs, and complete-well position to learn a bounded
   residual, followed by frozen within-well smoothing and shares.
4. A final held-excluded bounded ridge may combine only selection-clean
   positive outputs with C016.  Every active direction must be
   removal-necessary on selector roles.

For every held role, hyperparameters and weights are chosen only through
cross-fitting inside the other three development roles.  Public leaderboard
feedback, F4, confirmation regroupings, and quarantined artifacts are not
inputs.
