# V5 batch-2 run-16 source review

Run 16 begins after Run 14 established a stable but insufficient clean
four-fold parent (`+0.115568` versus protected V20).  F4, both confirmation
regroupings, and the remaining protected reveal remain sealed.

The active quarantine applies to
`purece_strat_clean.npz` and every transitive Run-10 through Run-13 artifact.
That archive selected a stratified checkpoint with an original-fold indexing
error.  Run 16 does not use the archive, its route features, its metrics, or
any downstream router.

The earlier A830 and A840 per-original-fold prediction banks precede the
quarantined selector.  Each prediction file embeds its original held fold,
checkpoint hash, source hash, gate hash, well layout, D570 parent, and false
F4/audit/confirmation flags.  The A830 bank is already pinned by the clean
C922 manifest.  A840 will receive a new metric-free manifest before any Run
16 target score.  A held fold's A830/A840 model was trained on the other
three development folds, so fixed snapshot/temperature predictions are
eligible original-fold-safe directions; the later stratified selection is
not.

## Registered mechanisms

1. Fit a bounded nonnegative ridge over the 30 fixed A830
   snapshot/temperature corrections and three inference-only low-frequency
   transforms.  Ridge weights for held role `h` are chosen only through
   cross-fitting inside the other three roles.
2. Repeat the identical procedure for the independently trained A840
   D570-prompted bank.
3. Combine only selection-clean A830, A840, D209, and Run14 directions in a
   final bounded held-excluded ensemble.  Every active direction must pass a
   removal-necessity check.

The public leaderboard and public per-well probes are not inputs to any
architecture, recipe, transform, or weight choice.
