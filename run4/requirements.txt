# Run4 needs everything v20 needs, plus the libraries the candidate bank,
# the routers, and the frozen kernel import.
-r ../requirements.txt

joblib>=1.3
xgboost>=2.0
lightgbm>=4.0

# Only the SegFormer candidates in the run-6 trajectory chain need these.
# See README.md "Pretrained backbone" before running that stage offline.
transformers>=4.40
torchvision>=0.16
opencv-python>=4.9
